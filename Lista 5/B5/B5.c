#include <stdio.h>
int main(){
    int i,a;
    char str[1000],inv[1000];
    scanf("%s",str);
    for(i=0;str[i]!='\0';i++){
    }
    for(a=0;i!=0;a++){
        i--;
        inv[a]=str[i];
    }
    inv[a]='\0';
    printf("%s\n",inv);
    return 0;
}